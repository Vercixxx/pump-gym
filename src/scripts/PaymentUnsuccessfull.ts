import { ref, computed } from 'vue';
import { usePiniaStorage } from '../store/pinia';

// Firebase
import { collection, setDoc, doc, Timestamp } from "firebase/firestore";
import { db } from '../firebase.js';


export const PaymentUnsuccessfull = async () => {

}
